import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SocialLoginComponent = () => {
  return (
    <View>
      <Text>SocialLoginComponent</Text>
    </View>
  );
};

export default SocialLoginComponent;

const styles = StyleSheet.create({});
